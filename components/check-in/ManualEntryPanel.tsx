import type { FormEvent } from "react";
import Button from "@/components/ui/Button";
import CheckInIcon from "./CheckInIcons";

export default function ManualEntryPanel({ value, checking, onChange, onSubmit }: { value: string; checking: boolean; onChange: (value: string) => void; onSubmit: (event: FormEvent<HTMLFormElement>) => void }) {
  return <section className="sep-card p-4 sm:p-6" aria-labelledby="manual-entry-title"><div className="flex items-start gap-4"><span className="grid h-12 w-12 shrink-0 place-items-center rounded-xl bg-slate-100 text-slate-700"><CheckInIcon name="pass" /></span><div><h2 id="manual-entry-title" className="sep-card-title">Manual Entry</h2><p className="sep-secondary mt-1">Verify a pass when scanning is not available.</p></div></div><form onSubmit={onSubmit} className="mt-6"><label htmlFor="eventPassId" className="sep-label block">Event Pass ID</label><input id="eventPassId" name="eventPassId" value={value} disabled={checking} onChange={(event) => onChange(event.target.value.toUpperCase())} placeholder="SEP-4UGK46" autoComplete="off" className="sep-control mt-2 font-mono text-lg font-bold uppercase tracking-wider" /><p className="sep-caption mt-2">Enter SEP-4UGK46 or simply 4UGK46.</p><Button type="submit" size="lg" loading={checking} className="mt-5 w-full">{checking ? "Checking…" : "Verify & Check In"}</Button></form><div className="mt-5 rounded-xl border border-[#e7e1d7] bg-stone-50 p-4"><p className="text-xs font-bold uppercase tracking-wide text-slate-500">Example pass</p><p className="mt-2 font-mono text-lg font-bold tracking-wider text-slate-900">SEP-4UGK46</p></div></section>;
}
