"use client";
import {useCallback,useEffect,useMemo,useState} from "react";
import {getAutomationCenterData,retryWorkflowEvent,type AutomationCenterData,type WorkflowRow} from "@/services/automationCenterService";
import {useAppLanguage} from "@/lib/i18n/useAppLanguage";

const copy={en:{title:"Automation Center",subtitle:"Monitor every event workflow from one operational workspace.",refresh:"Refresh",healthy:"Healthy",degraded:"Needs attention",health:"System Health",queue:"Workflow Queue",jobs:"Upcoming Jobs",active:"Active Automations",stats:"Processing Statistics",timeline:"Timeline",retry:"Retry Center",details:"Workflow Details",allEvents:"All events",allStatuses:"All statuses",pending:"Pending",processing:"Processing",processed:"Processed",failed:"Failed",successRate:"Success rate",avgAttempts:"Average attempts",dueSoon:"Due in 24 hours",noData:"No activity matches these filters.",retryNow:"Retry now",retrying:"Retrying…",close:"Close",attempts:"Attempts",available:"Available",source:"Source",entity:"Entity",lastError:"Last error",queueClear:"Queue is operating normally",sources:"Data sources",automations:["Pledge capture","Payment lifecycle","Reminder scheduling","Invitation delivery","RSVP tracking","Event check-in"]},sw:{title:"Kituo cha Automation",subtitle:"Fuatilia workflow zote za matukio katika sehemu moja ya uendeshaji.",refresh:"Onyesha upya",healthy:"Mfumo uko sawa",degraded:"Inahitaji uangalizi",health:"Afya ya Mfumo",queue:"Foleni ya Workflow",jobs:"Kazi Zijazo",active:"Automation Zinazotumika",stats:"Takwimu za Uchakataji",timeline:"Mfululizo wa Matukio",retry:"Kituo cha Majaribio Tena",details:"Maelezo ya Workflow",allEvents:"Matukio yote",allStatuses:"Hali zote",pending:"Inasubiri",processing:"Inachakatwa",processed:"Imekamilika",failed:"Imeshindikana",successRate:"Asilimia ya mafanikio",avgAttempts:"Wastani wa majaribio",dueSoon:"Ndani ya saa 24",noData:"Hakuna shughuli zinazolingana na vichujio hivi.",retryNow:"Jaribu tena",retrying:"Inajaribu…",close:"Funga",attempts:"Majaribio",available:"Itapatikana",source:"Chanzo",entity:"Rekodi",lastError:"Hitilafu ya mwisho",queueClear:"Foleni inaendelea vizuri",sources:"Vyanzo vya taarifa",automations:["Ahadi za michango","Mzunguko wa malipo","Ratiba za vikumbusho","Utumaji wa mialiko","Ufuatiliaji wa RSVP","Kuingia tukioni"]}} as const;
const relation=<T,>(value:T|T[]|null)=>Array.isArray(value)?value[0]??null:value;
const badge=(status:string)=>status==="failed"?"bg-red-100 text-red-800":status==="processing"||status==="queued"||status==="pending"||status==="recommended"?"bg-amber-100 text-amber-900":status==="processed"||status==="sent"||status==="delivered"||status==="completed"?"bg-emerald-100 text-emerald-800":"bg-slate-100 text-slate-700";
export default function AutomationCenterDashboard(){const {language}=useAppLanguage(),t=copy[language];const [data,setData]=useState<AutomationCenterData|null>(null),[loading,setLoading]=useState(true),[error,setError]=useState(""),[eventId,setEventId]=useState("all"),[status,setStatus]=useState("all"),[selected,setSelected]=useState<WorkflowRow|null>(null),[retrying,setRetrying]=useState<number|null>(null),[observedAt,setObservedAt]=useState(0);
 const load=useCallback(async()=>{setLoading(true);setError("");try{setData(await getAutomationCenterData());setObservedAt(Date.now())}catch(cause){setError(cause instanceof Error?cause.message:"Automation Center could not be loaded.")}finally{setLoading(false)}},[]);useEffect(()=>{const timer=setTimeout(()=>void load(),0);return()=>clearTimeout(timer)},[load]);
 void observedAt;
 /* eslint-disable react-hooks/purity -- operational health and due-soon counts are point-in-time snapshots */
 const workflows=useMemo(()=>data?.workflows.filter(item=>(eventId==="all"||item.event_id===Number(eventId))&&(status==="all"||item.status===status))??[],[data,eventId,status]),jobs=useMemo(()=>data?.jobs.filter(item=>eventId==="all"||item.event_id===Number(eventId))??[],[data,eventId]),timeline=useMemo(()=>data?.timeline.filter(item=>eventId==="all"||item.eventId===Number(eventId))??[],[data,eventId]);const counts={pending:workflows.filter(x=>x.status==="pending").length,processing:workflows.filter(x=>x.status==="processing").length,processed:workflows.filter(x=>x.status==="processed").length,failed:workflows.filter(x=>x.status==="failed").length},finished=counts.processed+counts.failed,success=finished?Math.round(counts.processed/finished*100):100,average=workflows.length?(workflows.reduce((sum,item)=>sum+item.attempt_count,0)/workflows.length).toFixed(1):"0.0",stale=workflows.filter(item=>item.status==="processing"&&Date.now()-new Date(item.created_at).getTime()>15*60_000).length,unhealthy=Boolean(data?.sourceErrors.length||counts.failed||stale);
 async function retry(item:WorkflowRow){setRetrying(item.id);try{await retryWorkflowEvent(item.id);await load();setSelected(null)}catch(cause){setError(cause instanceof Error?cause.message:"Retry failed.")}finally{setRetrying(null)}}
 return <div className="space-y-6"><header className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-xs font-black uppercase tracking-[.2em] text-emerald-700">Smart Event Pass</p><h1 className="mt-2 text-3xl font-black text-slate-950">{t.title}</h1><p className="mt-2 text-sm text-slate-600">{t.subtitle}</p></div><button disabled={loading} onClick={()=>void load()} className="min-h-12 rounded-xl bg-slate-950 px-5 font-bold text-white disabled:opacity-50">{loading?"…":t.refresh}</button></header>{error&&<p role="alert" className="rounded-xl border border-red-200 bg-red-50 p-4 text-red-800">{error}</p>}<div className="grid gap-3 sm:grid-cols-2"><label className="text-sm font-bold">{t.allEvents}<select value={eventId} onChange={e=>setEventId(e.target.value)} className="mt-2 min-h-12 w-full rounded-xl border bg-white px-3 font-normal"><option value="all">{t.allEvents}</option>{data?.events.map(event=><option key={event.id} value={event.id}>{event.title}</option>)}</select></label><label className="text-sm font-bold">{t.allStatuses}<select value={status} onChange={e=>setStatus(e.target.value)} className="mt-2 min-h-12 w-full rounded-xl border bg-white px-3 font-normal"><option value="all">{t.allStatuses}</option>{["pending","processing","processed","failed"].map(value=><option key={value} value={value}>{t[value as keyof typeof t] as string}</option>)}</select></label></div><section className={`rounded-2xl border p-5 ${unhealthy?"border-amber-200 bg-amber-50":"border-emerald-200 bg-emerald-50"}`}><div className="flex items-center justify-between gap-4"><div><h2 className="text-lg font-black">{t.health}</h2><p className="mt-1 text-sm">{unhealthy?t.degraded:t.healthy} · {data?.sourceErrors.length?`${data.sourceErrors.join(", ")} unavailable`:t.queueClear}</p></div><span className={`size-4 rounded-full ${unhealthy?"bg-amber-500":"bg-emerald-500"}`}/></div></section><section><h2 className="text-xl font-black">{t.stats}</h2><div className="mt-3 grid grid-cols-2 gap-3 lg:grid-cols-6">{[[t.pending,counts.pending],[t.processing,counts.processing],[t.processed,counts.processed],[t.failed,counts.failed],[t.successRate,`${success}%`],[t.avgAttempts,average]].map(([label,value])=><Stat key={String(label)} label={String(label)} value={value}/>)}</div></section><div className="grid gap-6 xl:grid-cols-[1.35fr_.65fr]"><section className="rounded-2xl border bg-white shadow-sm"><div className="border-b p-5"><h2 className="text-xl font-black">{t.queue}</h2></div><div className="max-h-[560px] overflow-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead className="sticky top-0 bg-slate-50 text-xs uppercase text-slate-500"><tr>{["Workflow","Event","Created",t.attempts,"Status"].map(x=><th key={String(x)} className="p-3">{x}</th>)}</tr></thead><tbody>{workflows.map(item=><tr key={item.id} onClick={()=>setSelected(item)} className="cursor-pointer border-t hover:bg-slate-50"><td className="p-3 font-bold">{item.event_type}</td><td className="p-3">{relation(item.events)?.title??"Event"}</td><td className="p-3">{new Date(item.created_at).toLocaleString()}</td><td className="p-3">{item.attempt_count}/5</td><td className="p-3"><Badge value={item.status}/></td></tr>)}</tbody></table>{!workflows.length&&<Empty text={t.noData}/>}</div></section><section className="rounded-2xl border bg-white p-5 shadow-sm"><h2 className="text-xl font-black">{t.retry}</h2><div className="mt-4 space-y-3">{workflows.filter(item=>item.status==="failed"&&item.attempt_count<5).map(item=><article key={item.id} className="rounded-xl border border-red-100 bg-red-50 p-4"><p className="font-bold">{item.event_type}</p><p className="mt-1 line-clamp-2 text-xs text-red-800">{item.last_error||"Processing failed."}</p><button disabled={retrying===item.id} onClick={()=>void retry(item)} className="mt-3 min-h-11 rounded-xl bg-red-700 px-4 text-sm font-bold text-white disabled:opacity-50">{retrying===item.id?t.retrying:t.retryNow}</button></article>)}{!workflows.some(item=>item.status==="failed"&&item.attempt_count<5)&&<Empty text={t.queueClear}/>}</div></section></div><div className="grid gap-6 xl:grid-cols-2"><section className="rounded-2xl border bg-white p-5 shadow-sm"><h2 className="text-xl font-black">{t.jobs}</h2><p className="mt-1 text-sm text-slate-500">{jobs.filter(item=>new Date(item.scheduled_for).getTime()<Date.now()+86400000&&new Date(item.scheduled_for)>new Date()).length} {t.dueSoon.toLowerCase()}</p><div className="mt-4 space-y-3">{jobs.filter(item=>["scheduled","recommended","queued"].includes(item.status)).slice(0,12).map(item=><article key={item.id} className="flex justify-between gap-3 rounded-xl border p-3"><div><p className="font-bold">{relation(item.event_pledges)?.full_name??item.schedule_type}</p><p className="mt-1 text-xs text-slate-500">{relation(item.events)?.title} · {new Date(item.scheduled_for).toLocaleString()}</p></div><Badge value={item.status}/></article>)}{!jobs.length&&<Empty text={t.noData}/>}</div></section><section className="rounded-2xl border bg-white p-5 shadow-sm"><h2 className="text-xl font-black">{t.active}</h2><div className="mt-4 grid gap-3 sm:grid-cols-2">{t.automations.map((label,index)=><article key={label} className="rounded-xl border p-4"><span className={`inline-block size-2 rounded-full ${index<3||timeline.some(item=>item.kind===(index===3?"invitation":index===4?"rsvp":"checkin"))?"bg-emerald-500":"bg-slate-300"}`}/><h3 className="mt-2 font-bold">{label}</h3><p className="mt-1 text-xs text-slate-500">{index<3?workflows.filter(item=>index===0?item.event_type.startsWith("pledge."):index===1?item.event_type.startsWith("payment."):item.event_type.includes("reminder")).length:timeline.filter(item=>item.kind===(index===3?"invitation":index===4?"rsvp":"checkin")).length} events</p></article>)}</div></section></div><section className="rounded-2xl border bg-white p-5 shadow-sm"><h2 className="text-xl font-black">{t.timeline}</h2><div className="mt-4 space-y-1">{timeline.slice(0,80).map(item=><article key={item.id} className="grid gap-1 border-l-2 border-emerald-300 py-3 pl-4 sm:grid-cols-[1fr_auto]"><div><p className="font-bold capitalize">{item.label.replaceAll("_"," ")}</p><p className="text-xs text-slate-500">{item.eventTitle} · {item.kind}{item.detail?` · ${item.detail}`:""}</p></div><div className="text-xs text-slate-500 sm:text-right"><Badge value={item.status}/><p className="mt-1">{new Date(item.occurredAt).toLocaleString()}</p></div></article>)}{!timeline.length&&<Empty text={t.noData}/>}</div></section>{selected&&<div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/60 p-4" role="dialog" aria-modal="true" aria-labelledby="workflow-title"><div className="w-full max-w-xl rounded-2xl bg-white p-6 shadow-2xl"><div className="flex justify-between gap-3"><div><p className="text-xs font-black uppercase text-emerald-700">{t.details}</p><h2 id="workflow-title" className="mt-1 text-xl font-black">{selected.event_type}</h2></div><button onClick={()=>setSelected(null)} aria-label={t.close} className="size-11 rounded-xl border text-xl">×</button></div><dl className="mt-5 grid gap-4 sm:grid-cols-2"><Detail label="Event" value={relation(selected.events)?.title??"Event"}/><Detail label="Status" value={selected.status}/><Detail label={t.source} value={selected.source}/><Detail label={t.entity} value={`${selected.entity_type}${selected.entity_id?` · ${selected.entity_id}`:""}`}/><Detail label={t.attempts} value={`${selected.attempt_count}/5`}/><Detail label={t.available} value={new Date(selected.available_at).toLocaleString()}/></dl>{selected.last_error&&<div className="mt-5 rounded-xl bg-red-50 p-4"><p className="text-xs font-bold uppercase text-red-700">{t.lastError}</p><p className="mt-2 break-words text-sm text-red-900">{selected.last_error}</p></div>}{selected.status==="failed"&&selected.attempt_count<5&&<button disabled={retrying===selected.id} onClick={()=>void retry(selected)} className="mt-5 min-h-12 w-full rounded-xl bg-red-700 font-bold text-white">{retrying===selected.id?t.retrying:t.retryNow}</button>}</div></div>}</div>}
function Stat({label,value}:{label:string;value:string|number}){return <article className="rounded-2xl border bg-white p-4 shadow-sm"><p className="text-xs font-bold text-slate-500">{label}</p><p className="mt-2 text-2xl font-black tabular-nums">{value}</p></article>};function Badge({value}:{value:string}){return <span className={`inline-flex rounded-full px-2.5 py-1 text-xs font-bold capitalize ${badge(value)}`}>{value.replaceAll("_"," ")}</span>};function Empty({text}:{text:string}){return <p className="p-8 text-center text-sm text-slate-500">{text}</p>};function Detail({label,value}:{label:string;value:string}){return <div><dt className="text-xs font-bold uppercase text-slate-400">{label}</dt><dd className="mt-1 break-words font-semibold capitalize">{value}</dd></div>}
